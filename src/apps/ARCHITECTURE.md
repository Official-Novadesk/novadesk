# Novadesk `src/apps` architecture

Mermaid diagrams for everything under [`src/apps`](.). View in GitHub, VS Code (Mermaid preview), or [mermaid.live](https://mermaid.live).

## 1. Applications and tooling (`src/apps`)

```mermaid
flowchart TB
  subgraph src_apps ["src/apps"]
    subgraph runtime ["novadesk/ — main desktop runtime"]
      NovadeskExe["novadesk.exe<br/>CMake: novadesk_runner"]
    end

    subgraph manager ["Widget lifecycle and packaging"]
      Nwm["nwm/<br/>nwm.exe CLI"]
      Manage["manage_novadesk/<br/>manage_novadesk.exe"]
      Restart["restart_novadesk/<br/>restart_novadesk.exe"]
    end

    subgraph install ["Installers"]
      Stub["installer_stub/<br/>installer_stub.exe"]
      Ndpkg["ndpkg_installer/<br/>ndpkg_installer.exe"]
    end

    Assets["assets/images/<br/>shared UI assets"]
  end

  User["User / developer"] --> Nwm
  User --> Manage
  User --> NovadeskExe

  Nwm -->|"init / run / build"| WidgetSrc["Widget project<br/>index.js + meta.json"]
  Nwm -->|"build: packages dist"| Dist["dist/ setup + payload"]
  Nwm -->|"embeds"| Stub
  Dist --> Stub
  Dist --> NovadeskExe

  Manage -->|"loads widgets"| NovadeskExe
  Manage -->|"install .ndpkg"| Ndpkg
  Ndpkg -->|"extract to Widgets/"| NovadeskExe
  Restart -->|"restarts"| Manage

  Stub -->|"self-extract install"| Installed["%ProgramFiles%/Novadesk"]
  Installed --> NovadeskExe
  Installed --> Manage
```

| Path | Role |
|------|------|
| [`novadesk/`](novadesk/) | Core widget runtime: layered windows, Direct2D UI, QuickJS scripting |
| [`nwm/`](nwm/) | CLI: `init`, `run`, `build`, `--install`; builds `.ndpkg` / setup using `installer_stub` |
| [`manage_novadesk/`](manage_novadesk/) | GUI manager: list/load widgets, tray integration |
| [`restart_novadesk/`](restart_novadesk/) | Helper to restart the manager process |
| [`ndpkg_installer/`](ndpkg_installer/) | GUI installer for `.ndpkg` widget packages |
| [`installer_stub/`](installer_stub/) | Small bootstrap EXE; payload appended by `nwm build` |
| [`assets/`](assets/) | Images used by manager/installer UIs |

> Build output under `src/apps/x64/` is generated by MSVC projects and is not part of the source layout.

---

## 2. `novadesk/` — layer overview

```mermaid
flowchart TB
  subgraph novadesk_app ["novadesk/"]
    Entry["Novadesk.cpp<br/>WinMain, tray, single-instance"]

    subgraph domain ["domain/"]
      DesktopManager["DesktopManager"]
      Widget["Widget<br/>windows, layout, animation"]
      AnimationEasing["AnimationEasing"]
      Chrome["WidgetWindowChromeHelper"]
      ContextMenu["WidgetContextMenuHelper"]
    end

    subgraph scripting ["scripting/quickjs/"]
      JSEngine["engine/JSEngine"]
      PropertyParser["parser/PropertyParser"]
      NovadeskModule["modules/NovadeskModule"]
      WidgetUi["modules/WidgetUiBindings"]
      WinEvents["modules/WidgetWindowEventBindings"]
      SystemMod["modules/SystemModule"]
      FSMod["modules/FSModule"]
      ModSys["modules/ModuleSystem"]
    end

    subgraph render ["render/"]
      ElementBase["Element"]
      D2D["Direct2DHelper"]
      Elements["Text, Image, Button, Bitmap,<br/>Bar, Line, RoundLine, Histogram,<br/>AreaGraph, Rotator, LayoutBox"]
      Shapes["ShapeElement + Rectangle, Ellipse,<br/>Line, Arc, Path, Curve"]
      Border["BoxBorderPaint / BoxBorderTypes"]
      Support["FontManager, CursorManager,<br/>Tooltip, GeneralImage"]
    end

    subgraph shared ["shared/"]
      Settings["Settings"]
      Logging["Logging"]
      Utils["Utils, PathUtils, FileUtils"]
      Menu["MenuUtils, MenuItem"]
      ColorUtil["ColorUtil"]
      SystemAPI["System"]
    end
  end

  WidgetJS["Widget JavaScript<br/>index.js, script.ui.js"] --> JSEngine
  JSEngine --> NovadeskModule
  NovadeskModule --> WidgetUi
  NovadeskModule --> WinEvents
  NovadeskModule --> SystemMod
  NovadeskModule --> FSMod
  WidgetUi --> PropertyParser
  WidgetUi --> Widget
  WinEvents --> Widget
  Entry --> DesktopManager
  Entry --> Widget
  Entry --> JSEngine
  Widget --> ElementBase
  Widget --> AnimationEasing
  Widget --> Chrome
  Widget --> ContextMenu
  ElementBase --> D2D
  Elements --> ElementBase
  Shapes --> ElementBase
  Border --> Elements
  PropertyParser --> shared
  Widget --> shared
  D2D --> FontManager
```

---

## 3. `novadesk/render/` — element and shape tree

```mermaid
classDiagram
  class Element {
    position, size, rotate
    solid color / gradient
    bevel, hit-test, events
  }

  class ShapeElement {
    stroke, fill, paths
  }

  Element <|-- TextElement
  Element <|-- ImageElement
  Element <|-- ButtonElement
  Element <|-- BitmapElement
  Element <|-- BarElement
  Element <|-- LineElement
  Element <|-- RoundLineElement
  Element <|-- HistogramElement
  Element <|-- AreaGraphElement
  Element <|-- RotatorElement
  Element <|-- ShapeElement

  ShapeElement <|-- RectangleShape
  ShapeElement <|-- EllipseShape
  ShapeElement <|-- LineShape
  ShapeElement <|-- ArcShape
  ShapeElement <|-- PathShape
  ShapeElement <|-- CurveShape
  ShapeElement <|-- ElementLayoutBox

  ElementLayoutBox ..> BoxBorderPaint : borders
  Element ..> Direct2DHelper : draw
  Element ..> FontManager : text
  Element ..> CursorManager : cursors
  Widget ..> Element : owns many
```

---

## 4. Script → screen data flow

```mermaid
sequenceDiagram
  participant JS as Widget JS
  participant Engine as JSEngine
  participant UI as WidgetUiBindings
  participant Parser as PropertyParser
  participant W as Widget
  participant E as Element
  participant D2D as Direct2DHelper

  JS->>Engine: ui.addShape / ui.animate
  Engine->>UI: native binding
  UI->>Parser: parse options object
  Parser-->>UI: ShapeOptions / AnimationOptions
  UI->>W: AddShape / StartElementAnimation
  W->>E: create or update
  Note over W: timer StepAnimations
  W->>E: SetPosition / SetSize / SetRotate
  W->>D2D: Redraw layered window
```

---

## 5. `nwm` CLI commands

```mermaid
flowchart LR
  subgraph nwm_cli ["nwm.exe"]
    Init["init widgetName"]
    Run["run"]
    Build["build"]
    Install["--install"]
  end

  Init --> Template["Widget template<br/>meta.json + index.js"]
  Run --> Novadesk["Launch Novadesk.exe<br/>with widget cwd"]
  Build --> Zip["Zip widget + addons"]
  Build --> Sfx["Append payload to<br/>installer_stub.exe"]
  Install --> Sfx
```

---

## Source file index (by folder)

### `novadesk/domain/`
`Novadesk.cpp`, `DesktopManager.cpp`, `Widget.cpp`, `AnimationEasing.cpp`, `WidgetWindowChromeHelper.cpp`, `WidgetContextMenuHelper.cpp`

### `novadesk/scripting/quickjs/`
- **engine:** `JSEngine.cpp`
- **parser:** `PropertyParser.cpp`
- **modules:** `NovadeskModule.cpp`, `WidgetUiBindings.cpp`, `WidgetWindowEventBindings.cpp`, `SystemModule.cpp`, `FSModule.cpp`, `ModuleSystem.cpp`

### `novadesk/render/`
`Element`, `ShapeElement`, layout/border (`ElementLayoutBox`, `BoxBorderPaint`), primitives (`RectangleShape`, `EllipseShape`, `LineShape`, `ArcShape`, `PathShape`, `CurveShape`), widgets (`TextElement`, `ImageElement`, `ButtonElement`, `BitmapElement`, `BarElement`, `LineElement`, `RoundLineElement`, `HistogramElement`, `AreaGraphElement`, `RotatorElement`), helpers (`Direct2DHelper`, `FontManager`, `CursorManager`, `Tooltip`, `GeneralImage`)

### `novadesk/shared/`
`Settings`, `Logging`, `Utils`, `PathUtils`, `FileUtils`, `MenuUtils`, `MenuItem`, `ColorUtil`, `System`

### Other apps
- **nwm:** `src/main.cpp`, `src/rescle.cc`
- **manage_novadesk:** `main.cpp`
- **restart_novadesk:** `main.cpp`
- **ndpkg_installer:** `main.cpp`
- **installer_stub:** `src/installer_stub.cpp`
